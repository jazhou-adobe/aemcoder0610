export default async function decorate(block) {
  // The block has 3 rows:
  // Row 1: Heading + subtitle
  // Row 2: Category pill links
  // Row 3: CTA buttons (View all courses, Take a quiz)
  // No structural changes needed — CSS handles layout via flex on the cells.
}
